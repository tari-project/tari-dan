[package]
name = "tari_template_lib"
description = "Tari template library provides abstrations that interface with the Tari validator engine"
version = "0.32.0"
edition.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true

[dependencies]
tari_template_abi = { workspace = true, default-features = false }
tari_template_lib_types = { workspace = true, default-features = false }
tari_template_macros = { workspace = true, optional = true }
tari_bor = { workspace = true, default-features = false }
minicbor = { workspace = true, default-features = false, features = ["alloc", "derive"] }

serde = { workspace = true, default-features = false, optional = true, features = ["derive"] }
ts-rs = { workspace = true, optional = true }
borsh = { workspace = true, optional = true, features = ["derive"] }

[features]
default = ["macro", "std"]
macro = ["tari_template_macros"]
std = ["minicbor/std", "serde?/std", "tari_bor/std", "tari_template_abi/std", "tari_template_lib_types/std"]
alloc = ["serde?/alloc", "tari_bor/alloc", "tari_template_abi/alloc", "tari_template_lib_types/alloc"]
serde = ["dep:serde", "tari_bor/serde", "tari_template_abi/serde", "tari_template_lib_types/serde"]
ts = ["std", "serde", "ts-rs", "tari_bor/ts", "tari_template_abi/ts", "tari_template_lib_types/ts"]
borsh = ["dep:borsh", "tari_template_lib_types/borsh", "tari_bor/borsh"]
extra-arith = ["tari_template_lib_types/extra-arith"]
precision = ["tari_template_lib_types/precision"]
extra-maps = ["tari_template_lib_types/extra-maps"]
# Times the `tari_bor::Value` conversions an `InvokeResult` performs, so a host
# measuring the ABI boundary can separate them from the codec itself. Host-side
# measurement only (see `tari_engine::abi_metrics`); it needs a thread-local, so
# it implies `std` and is never on in a template build.
metrics = ["std"]
